{% extends 'layoutAdmin.html.twig' %}

{% block body %}
   
                        <div class="job-bx job-profile bg-white">
                           

                             <div class="job-bx-title clearfix">
                                <h5 class="font-weight-700 float-start text-primary">Équipe RH ({{RH|length}})
                                </h5>
                                  <div class="float-end">

                                <a href="{{path('RH_Add')}} " class="btn btn-primary rounded-pill add-btn btn-sm"><i class="fas fa-plus fa-sm "></i> Ajouter</a>
                            </div>
                            </div>
                                      {% for message in app.flashes('success') %}
            <div class="text-center text-dark alert alert-success">
                {{ message }}
            </div>
            {% endfor %}
                {% for message in app.flashes('error') %}
            <div class="text-center text-dark alert alert-warning">
                {{ message }}
            </div>
        {% endfor %}
                          <div class="row">
                                            <div class="">
                                                 {% for user in RH %}
                                                <ul class=" post-job-bx browse-job-grid post-resume row ">
                                                    <li class="col-lg-12 col-md-6 ">
                                                        <div class="post-bx bg-200 border-3 bg-white  m-b30">
                                                            <div class="">
                                                                <div class="job-post-info">
                                                                    <h5 class="tecos-h">{{user.fname}} {{user.lname}} <span class="text-secondary"> ({{user.email}})</span> </h5>
                                                                </div>
                                                            </div> <label class="like-btn"><a href="{{path('dashboard_managementEdit',{'id':user.id})}}" class=" btn btn-outline-primary add-btn btn-sm"><i class="fas fa-pencil-alt m-r4"></i> </a><span> <a href="{{path('RH_delete',{'id':user.id})}}" onclick = "return confirm('Voulez-vous vraiment supprimer cet utilisateur ?')" class="  btn btn-outline-danger add-btn  btn-sm"><i class="fas fa-trash" ></i></a></span>
                                                         
                                                        </div>
                                                    </li>
                                                 
                                                </ul>
                                                  {% endfor %}

                                            </div>
                                        </div>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
 

 













         
{% endblock %}